import 'package:flutter/material.dart';

class Notifyy extends StatelessWidget {
  const Notifyy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
